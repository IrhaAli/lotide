/**
 * Returns the head (ie. first item) of the array.
 * @param {array} array
 * @return {array} the first element of the array.
 */

const head = function(array) {
  return array[0];
};

module.exports = head;